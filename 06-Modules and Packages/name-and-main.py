# when you run a script from the command line,
# all code at identation level 0 gets run
# functions get defined, classes get defined, conditionals are run etc

# __name__ is a built in dunder variable, that records how a script is being run
# if a script is run from the command line, it gets assigned __name__ == "__main__"
__name__